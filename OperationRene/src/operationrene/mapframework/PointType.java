package operationrene.mapframework;

public enum PointType {
    // The public enum defines which kind the point of interest is

        Key,
        Safe,
        Minigame,
        AlarmArea,
        Door,
        EntryPoint,
        SecurityCamera,
        Guard

}
